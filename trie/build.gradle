version = '1.0.0'

sourceCompatibility = 1.8
targetCompatibility = 1.8

ext {
    projectDescription = "Lightning Fast Java Map implementation"
}

dependencies {
    api project(':mutatio')
}

apply from: "$rootDir/gradle/publishing.gradle"
